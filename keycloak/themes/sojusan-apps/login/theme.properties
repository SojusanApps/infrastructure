parent=keycloak.v2
import=common/keycloak

styles=css/styles.css

# Follows prefers-color-scheme. styles.css supplies its own Pitch Black
# dark-mode values under .pf-v5-theme-dark rather than v2's stock dark
# tokens — see my-game-list-frontend's src/css/colors.css for the source
# palette this mirrors.
darkMode=true
