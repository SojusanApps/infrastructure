parent=keycloak.v2
import=common/keycloak

styles=css/styles.css

# The v2 base theme's PatternFly dark-mode tokens weren't designed with this
# brand palette in mind, so pin a single light look instead of following
# prefers-color-scheme.
darkMode=false
